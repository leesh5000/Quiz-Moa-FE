import styled from "styled-components";
import palette from "./palette";

export const SortingButtonStyle = styled.button`
  font-size: 1.125rem;
  font-weight: 700;
  color: ${palette.gray[6]};
  border: none;
  cursor: pointer;
  background: none;
  margin-right: 1rem;
  padding-bottom: 0.35rem;
  letter-spacing: 1.25px;
`;